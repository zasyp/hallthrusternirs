# Dimensionless diagnostics and paper/SI scaling.
# Loads: DiagnosticsMetrics (sim/report helpers), PaperScales (SI → closures and plot scaling).
include("diagnostics_metrics.jl")
include("paper_scales.jl")
